# Environment variables for speckitlab.
# Copy this file to `.env.local` (git-ignored) and fill in values.
# The site has no runtime env requirements as of today — this file
# exists as a safety guardrail so future additions follow the pattern.

# Example (commented — no live usage):
# NEXT_PUBLIC_ANALYTICS_ID=
# CONTACT_FORM_ENDPOINT=
